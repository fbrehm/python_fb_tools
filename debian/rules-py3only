#!/usr/bin/make -f
# -*- makefile -*-

# BIN_PKGNAME := fb-tools
# LIB_PKGNAME := python3-fb-tools

export PYBUILD_NAME=fb-tools

# INSTALL_DIR_BIN  := $(CURDIR)/debian/$(BIN_PKGNAME)
# INSTALL_DIR_LIB := $(CURDIR)/debian/$(LIB_PKGNAME)

# PYTHONS := $(shell py3versions -vr || true)

export PYBUILD_DESTDIR_python3=debian/python3-fb-tools/
export PYBUILD_DISABLE=test/python

%:
	dh $@ --with python3 --buildsystem=pybuild

# override_dh_auto_clean:
# 	@echo
# 	@echo "-------------------------------------------"
# 	@echo "Cleaning ..."
# 	@echo
# 	dh_auto_clean
# 	rm -rf dist
# #	rm -fv man/*
# #	rm -fv tmp/*

# override_dh_auto_build:
# 	@echo ""
# 	@echo "Building Python 3 stuff ..."
# 	set -e && for pyvers in $(PYTHONS); do python$$pyvers setup.py build; done
# #	@echo ""
# #	./generate-manpages.sh

# override_dh_auto_install:
# 	@echo ""
# 	@echo "Installing Python 3 stuff ..."
# 	set -e && for pyvers in $(PYTHONS); do python$$pyvers setup.py install --root=$(INSTALL_DIR_LIB) --install-layout=deb; done
# 	@echo ""
# 	@echo "Installing bin package ..."
# 	install -d $(INSTALL_DIR_BIN)/usr
# 	mv -v $(INSTALL_DIR_LIB)/usr/bin $(INSTALL_DIR_BIN)/usr
# #	@echo ""
# #	@echo "Manpages ..."
# #	dh_installman

